import chai from 'chai';
import { read } from '../../src/action/read';

const assert = chai.assert;

describe('Test read', () => {
  it('should correctly read', async () => {
    // const ans = await read();
    assert(true);
  });
});
